import React from 'react';

export default () => (
  <div className="modal-about">
    <h6>Share Link</h6>
    <p>The current map view & active filters will be shared via this URL.</p>
    <p>TO DO: Construct URL with current filters</p>
  </div>
);
